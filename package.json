{
  "name": "Kariger.Com",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "build": "vite build"  // New script added
  },
  "dependencies": {
    "react": "^17.0.2",      // New dependency added
    "react-dom": "^17.0.2",
    "vite": "^4.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^1.0.0"
  }
}
git add package.json
git commit -m "Add build script and react dependency"
git push origin main  # or your branch name
